<div class="task-container">
    <!-- Add a task input and button -->
    {# <div class="add-task my-4">
        <input type="text" id="taskInput" placeholder="Task name">
        <button id="addTaskButton" class="custom-button bg-accent text-white rounded px-4 py-2">Add a task</button>
    </div> #}
    <!-- Show folder's tasks -->
    <div class="task-list" id="taskList">
        {% for task in tasks %}
        <div class="task-item mb-4 p-4 bg-gray-200 rounded shadow">
            <h3 class="task-title font-bold mb-2">{{ task.name }}</h3>
            <p class="task-description mb-2">{{ task.description }}</p>
            <div class="task-controls">
            </div>
        </div>
        {% endfor %}
    </div>
</div>


{% block javascripts %}
    {{ encore_entry_script_tags('tasks') }}
{% endblock %}